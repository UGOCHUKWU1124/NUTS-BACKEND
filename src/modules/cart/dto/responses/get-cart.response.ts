import { CartResponseDto } from '../cart-response.dto';

/**
 * Response for GET /cart
 * Returns the full cart with all items and metadata.
 */
export class GetCartResponseDto extends CartResponseDto {}
