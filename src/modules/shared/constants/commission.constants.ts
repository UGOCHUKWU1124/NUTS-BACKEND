/**
 * Creator commission rate — the fraction of each order item's revenue
 * credited to the creator. 0.85 = 85% to creator, 15% platform fee.
 *
 * Update this single constant if the rate ever changes.
 */
export const CREATOR_COMMISSION_RATE = 0.85;
